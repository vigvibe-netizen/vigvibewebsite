{
  "name": "vigvibe-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "node scripts/setup-routes.mjs",
    "routes": "node scripts/setup-routes.mjs",
    "dev": "npm run routes && astro dev",
    "build": "npm run routes && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.1",
    "astro": "^5.2.5"
  }
}
